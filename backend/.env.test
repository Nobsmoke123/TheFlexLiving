# Test Environment Configuration
# This file should be used for testing only

# Database
DB_PATH=./config/test-database.db

# Hostaway API (use mock data for tests)
ACCESS_TOKEN=test_token_for_mocking

# Server Configuration
PORT=3001
NODE_ENV=test

# Enable test mode to use mock data
TEST_MODE=true
